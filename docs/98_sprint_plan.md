# Current Sprint

## 최신 승인: 다음 초록에서 즉시 전환

- 다음 곡은 초록 시작 전 최대 10초부터 페이드인한다.
- 다음 곡이 초록 시작에 도달하는 순간 이전 곡을 즉시 음소거하고 다음 곡으로 전환한다. DJ/1절 모드에 함께 적용한다.

## 이전 승인: 비대칭 DJ 페이드 (이번 전환 방식으로 대체)

- 초록 경계 연결은 유지하고 다음 곡은 최대 10초에 걸쳐 진입, 이전 곡은 전환 후 최대 4초에 걸쳐 퇴장한다. 퇴장 규칙은 이번 승인에서 즉시 음소거로 대체했다.
- DJ/1절 모드에 함께 적용하며 짧은 구간은 실제 가용 길이로 제한한다.

## 최신 승인: 구조 구간에 초록 핸들 맞추기

- 자동 초록 시작은 인트로 마지막, 끝은 아웃트로 시작을 우선한다. 해당 라벨이 없는 쪽은 기존 드럼 경계로 대체한다.
- 잘못된/역전된 구간은 기존 경계로 대체하고 수동값은 유지한다. DJ도 화면과 동일한 자동 경계를 사용한다.

## 최신 승인: MLX 구조 분석 적용

- Mac 별도 MLX 환경으로 후렴/벌스 구간을 생성하고 기존 FIFO 작업 안에서 한 곡씩 실행한다.
- 드럼 경계/음파/BPM과 수동 편집은 보존한다. 새 분석 실패 시 기존 분석을 저장하고 같은 버전을 무한 재시도하지 않는다.
- 후렴 전에 verse가 있으면 첫 후렴 끝, 후렴부터 시작하면 두 번째 후렴 끝을 1절 후보로 삼는다. 인접 후렴은 합친다.
- 음파에 구간 라벨을 표시한다. Oracle는 변경하지 않고 로컬 통합 검증 후 관련 두 저장소를 푸시한다.

## 최신 승인: 재생바 시간 표시

- 양끝에 시작 0:00과 곡 전체 길이를 표시한다. 확대 중에도 전체 길이를 유지한다.
- 세 핸들 아래 이름을 제거하고 분:초 시간만 표시한다. 재생·분석·저장은 변경하지 않는다.

## 최신 승인: 재생바·음파 통합

- 재생 슬라이더를 음파 바로 위로 통합하고 드래그/음파 클릭 시 노란 재생선을 즉시 갱신하며 초 단위 seek를 전달한다.
- 초록 인트로/아웃트로와 흰 1절 경계선을 추가한다. 휠 확대, 가운데 버튼 드래그 이동을 제공한다.
- 확대 도구 행과 하단 안내/재생 위치 문구를 없애고 자동 분석 상태를 핸들 시간 아래에 배치한다.

## 최신 승인: 음파 편집 패널 정리

- 제목·수동 분석 버튼·완료/큐/DJ 설명·BPM/신뢰도·범례·상세 PCM 설명·저장/복원 버튼을 제거한다.
- 자동 분석 상태와 오른쪽 일시정지/재개 아이콘, 확대 도구와 재생 위치는 유지한다.
- 세 핸들 아래에 시간이 따라다니고 가까운 표시는 줄을 나눈다. 편집 종료 자동 저장과 오류 안내는 유지한다.

## 최신 승인: 명시적 확대·실제 상세 음파

- 자동 확대/손을 놓을 때 복귀를 제거하고 ＋/−/전체 보기와 커서 중심 휠 확대를 제공한다. 배율은 편집 중 유지한다.
- 기존 자리에서 1~64배로 확대하고 좌우 이동한다. 세 핸들·독립 1절·저장 계약을 유지한다.
- 분석 서버가 PCM 최소/최대 피크를 약 1ms 간격으로 저장하고 표시 구간/너비에 맞게 집계한다. 요약 파형을 늘려 상세 데이터로 가장하지 않는다.
- 구버전 곡은 기존 단일 큐로 재분석하며 수동값은 보존한다. Oracle 배포는 변경하지 않는다.

## 최신 수정: 확대 자동 복귀

- 포인터를 놓거나 취소/창 포커스를 잃으면 전체 음파로 복귀한다. 이동한 값은 정상 release 시 저장한다.
- 키보드로 연 확대는 전체 보기/Escape로 닫는다.

## 최신 승인: 기존 음파 자리에서 확대

- 별도 상세 행을 없애고 기존 음파 뷰포트 안에서 확대한다. 페이지 높이와 조절 위치를 유지한다.
- 같은 영역의 핸들로 편집하고 전체 보기/Escape로 복귀한다. 초록 선택 표시와 독립 1절 저장은 유지한다.

## 최신 승인: 즉시 음파 확대·독립 1절·반복 후보

- 미분류 구간 번호를 숨긴다. 삼각형을 잡으면 즉시 280ms 확대하며 작은 전체 음파와 큰 상세 음파를 함께 표시한다.
- 상세 음파는 요약점을 연결한 SVG로 표시한다. 새 PCM 해상도를 만들어내는 기능은 아니다.
- 초록 경계 편집이 1절 위치를 이동·재계산하지 않게 하고 저장 후에도 유지한다.
- 기존 후렴 후보가 부족하면 음파 반복 비교로 후보를 찾고 도입부 후보를 건너뛰어 후렴 끝을 제안한다.

## 최신 승인: 초록 경계 연결·DJ 1절

- 버튼은 꺼짐 → DJ → DJ 1절 → 꺼짐으로 순환한다. 초 설정 UI를 제거한다.
- 앞 곡 종료 경계와 다음 곡 초록 시작을 맞춘다. 인트로는 경계 이전, 아웃트로는 이후에 독립적으로 최대 10초 페이드한다.
- 1절 모드는 흰 핸들 위치를 종료 경계로 사용하며 그 뒤 최대 10초 잔여 소리만 페이드아웃한다.
- 꺼짐은 전체 곡 순차 전환이다. 기존 수동 구간과 데이터 저장 구조는 보존한다.

## 최신 승인: 재생목록 자동 순차 분석·1절 후보

- 현재 목록의 미분석·구버전 결과를 한 곡씩 자동 분석한다. 기존 자동 POST 금지는 이 범위에서 변경한다.
- 일시정지, 연결 실패 재시도, 10분 초과 제외와 세션 내 실패 반복 방지를 제공한다.
- 초반 후렴이면 두 번째 후렴 끝, 앞에 일반 구간이 있으면 첫 후렴 끝을 1절 후보로 제안한다.
- 수동 경계·명시적 1절 위치를 보존한다. 1절 재생 cutoff는 이번 범위가 아니다.

## 최신 승인: 3초 정지 정밀 확대·디제잉 복구

- 세 핸들을 누른 채 3초 움직이지 않으면 주변 확대 편집창을 연다. 이동하면 타이머 재시작.
- 원래 드래그 축은 바꾸지 않고 확대창에서 최대 20초 범위·0.01초 단계로 편집한다.
- 다음 곡 기존 분석 결과 미조회와 실패 곡 후보 선택을 수정한다. 새 분석 작업은 자동 등록하지 않는다.
- 디제잉 상태·순차 전환 이유를 표시하고 기존 재생·사용자 데이터는 유지한다.

## 최신 승인: 세 핸들·디제잉

- 초록 시작/끝 삼각형과 흰색 1절 삼각형·선을 표시한다. 초록색은 선택된 안쪽만 채운다.
- `verseEnd`를 선택적 수동 필드로 저장하며 초기 위치는 중앙이다. 1절 재생 모드는 후속 단계다.
- 디제잉 활성 시 앞 곡 초록 종료부터 양쪽 비초록 영역에서만 페이드한다.
- 시간 슬라이더는 최대 페이드 시간이다. 구간 미확인은 순차 재생, 수동값은 세션 자동값보다 우선한다.
- 기존 고정 겹침 fallback과 분석값 미사용 계약은 이 승인 범위에서 아래 새 계약으로 변경한다.

## 최신 승인: 음파와 드럼 구간 조절 통합

- 별도 초록 선택 띠를 없애고 음파 아래에 두 핸들을 배치한다.
- 드래그 중 같은 음파 위 초록 테두리를 즉시 갱신한다. 저장·복원·재생 계약은 유지한다.
- 메인과 Lab에 동일 적용하며 자동 분석 원본은 바꾸지 않는다.

## 최신 승인: 전체 음파·드럼 경계·후렴 후보

- 실제 PCM 전체 음파 요약과 반복 스펙트럼 기반 후렴 후보를 생성한다. 알 수 없는 구간은 일반 구간으로 남긴다.
- 인트로는 첫 추정 드럼 전, 아웃트로는 마지막 추정 드럼 후다. 드럼 검출 실패를 무드럼 판정으로 취급하지 않는다.
- 기존 박자 선 대신 음파·색상 구역을 표시하고 재생 위치 조회를 보완한다.
- 이번 단계는 향후 믹스용 분석 정보다. 후렴 자동 진입·자동 DJ 실행·기존 이어듣기 변경은 포함하지 않는다.
- 새 분석기 버전으로 이전 결과를 명시적 재분석하며 로컬 검증 후 커밋·푸시·배포한다.

## 최신 승인: 분석 그래프 가독성·현재 위치

- 박자·마디 그래프를 확대하고 초록 선택 띠를 축소한다. 손잡이 조작 크기는 유지한다.
- 기존 재생 진행 갱신 경로로 그래프에 현재 위치와 시간을 표시한다. 재생·분석 알고리즘은 바꾸지 않는다.

## 최신 승인: 일반 주소의 로컬 분석 기본값

- 비공개 개인 사용 단계이므로 별도 링크 없이 일반 마감이즈커밍에서 Mac 로컬 분석을 사용한다.
- 이전의 운영 기본값 유지 조건은 이번 승인으로 변경한다. Oracle는 명시적 쿼리에서만 사용한다.
- 큐·10분 제한·사이트 로그인·재생 계약은 유지한다. 서버 연결 실패 안내를 보완하고 배포한다.

## 추가 승인: 분석 큐 표시·연속 등록

- 분석 버튼은 현재 곡을 기존 서버 단일 FIFO 큐에 넣는다. 동시에 처리하는 곡은 1개다.
- 곡 전환과 관계없이 제출된 작업을 추적하고, 화면에 서버 전체 실행·대기 곡 수를 표시한다.
- 로컬과 Oracle의 같은 API 계약을 유지하며, 검증 후 관련 두 저장소를 커밋·푸시한다.
- 재생·자동 DJ 적용·10분 제한·환경 기본값은 바꾸지 않는다.

## 현재 우선순위: Mac 로컬 분석 복구

- 사용자의 후속 요청에 따라 이 Mac에서 분석 서비스를 실행하고
  `https://magamiscom.ing/?mediaAnalysis=local`로 기존 앱을 연결한다.
- 실패한 두 곡의 실제 로컬 분석으로 확인한다. 운영 기본 주소나 기존 Oracle 서비스는 변경하지 않는다.
- Oracle에서 모든 앱 사용자가 URL 분석을 안정적으로 사용하는 작업은 로컬 검증 다음 단계다.

## 2026-09-04 추가 승인: 기존 Oracle E2 분석 연결

- 사용자가 분석 버튼에서 Oracle로 해당 곡을 분석하도록 요청했다. 기존 A1 대기와 별개로,
  E2에 독립 분석 서비스와 기존 HTTPS 서버의 `/media-analysis/` 경로만 추가한다.
- 기존 앱의 코드·데이터·서비스를 이전하거나 중단하지 않는다. 기존 E2 변경 금지는 이번에
  승인된 분석 서비스 설치와 프록시 경로 추가 범위에서만 해제한다.
- 코드 구현·Oracle 설치·실제 1곡 분석은 완료했다. 후속 요청에 따라 모든 앱 로그인 사용자에게
  개방하고 10분 초과 곡을 거부한다. 두 저장소 커밋·푸시는 승인됐으며 로그인 브라우저 최종 검증은 남아 있다.
- 최신 검증과 배포 상태는 `99_recent_task.md`를 따른다.

## 상태

Sprint 17의 Apple Silicon 로컬 구현과 마감이즈커밍 연결을 완료했다. 분석 서비스, 실제
1곡 검증, 분석 편집 UI와 수동 pair 기반 이어듣기 시점까지 구현했다. 오라클 A1 배포는
인스턴스 가용 용량 확보 전까지 대기한다. 메인 앱과 독립된 localStorage 기반 노동요 Lab도
완료해 OAuth·Firebase 없이 실제 분석과 보정 흐름을 먼저 검증할 수 있다.

## Sprint 17 목표

- `/Users/bacbaqui/Desktop/code/media-analysis-service`에 범용 분석 API를 구축한다.
- 작업 상태·단일 큐, yt-dlp·FFmpeg 경계와 BPM·비트·드럼 분석을 구현한다.
- 실제 1곡과 고정 fixture로 결과·실패·임시 파일 정리를 검증한다.
- 마감이즈커밍에 환경별 API Client, 분석 상태와 편집 가능한 곡 지도를 연결한다.
- 분석 실패와 서버 부재가 기존 YouTube 재생을 막지 않게 한다.
- 기존 E2 서버와 기존 서비스를 수정하거나 이전하지 않는다.

## Task 순서

1. 완료: 로컬 서비스 기반·health check
2. 완료: 작업 계약·단일 큐·상태 저장·취소
3. 완료: yt-dlp·FFmpeg Adapter와 분석 알고리즘
4. 완료: 실제 로컬 분석과 임시 파일·실패 검증
5. 완료: 마감이즈커밍 API Client·상태 UI·곡 지도 편집
6. 완료: 로컬 통합 검증·문서·A1 배포 준비
7. 완료: 독립 노동요 Lab·localStorage 저장·실제 1곡 분석과 수동 구간 복원 검증
8. 완료: SQLite batch·결과 cache, Lab 전곡 분석 중단·재개 UI와 실제 5곡 시험 분석

실제 A1 생성·배포·DNS 전환은 로컬 Task 완료 판정과 별개이며 아직 실행하지 않았다.

각 Task는 동일한 서브 에이전트가 순차 수행하고 루트 에이전트가 diff와 검증 결과를 직접
점검한 뒤 다음 Task로 진행한다.

## 완료 조건

- 로컬 서비스와 마감이즈커밍의 관련 자동 검사가 통과한다.
- API 스키마, 분석기 버전, 자동·수동 데이터와 환경별 주소 경계가 문서화된다.
- macOS ARM64에서 실제 로컬 API와 1곡 분석을 확인한다.
- Ubuntu ARM64 배포 스크립트와 운영 설정은 비밀값 없이 준비한다.
- A1 서버 미생성 시 배포 미실행을 명시하고 기존 재생 fallback을 확인한다.

## 현재 재생 적용 범위

- 두 곡 모두 저장된 수동 드럼 구간이 유효할 때만 수동 계산 시각에 다음 곡을 시작한다.
- crossfade 길이는 기존 fixed seamless 설정을 유지한다.
- 수동값이 없거나 한쪽뿐이거나 범위를 벗어나면 기존 fixed seamless 계산을 그대로 사용한다.
- detected 분석값은 편집 참고용이며 실제 자동 재생에는 아직 사용하지 않는다.

## 이전 Sprint 완료 기록

## 전체 Sprint 종료 결과

- Sprint 1~5: 저장·초기화 계약, AppComposer, 렌더 경계, Metadata·File Port와 Adapter 분리
- Sprint 6~12: 뽀모도로·메모·프로필·MainTabs·CLIP·북마크·달력·노동요 Engine 구조 전환
- Sprint 13: 공개 전역과 공통 저장 서비스 책임 정리
- Sprint 14~16: 작은 탭, CLIP·북마크·달력, 노동요·App 공통 CSS 소유권 분리
- 완료 기록: `docs/completed/003_runtime_contract_baseline.md`~`docs/completed/018_workmusic_app_styles_refactor_complete.md`

## 완료 범위

- 노동요 목록·내부 탭·커버 흐름·플레이어·진행 표시·두 Player 이어듣기·리모컨·반응형 CSS를 `styles/workmusic.css`로 순수 이동
- App Shell·공통 탭·공통 모달·알림·footer·Drive 표시와 전역 반응형 CSS를 `styles/app.css`로 이동
- 빈 `styles.css`와 기존 로드 항목 제거
- 전체 Architecture, Source Map, Next Sprint, Recent Task와 Completed 기록 최종 동기화

## 완료 판정

- 이동 직후 원본 2,006줄과 App 422줄·노동요 1,584줄의 소유권별 바이트 동등성을 확인했다.
- 자동 테스트 62개, ESLint, Prettier와 diff 검사를 통과했다.
- 데스크톱 1280×900과 390×844 모바일에서 7개 기본 탭, 프로필·사용자 정의 탭, 공통 모달과 노동요 빈 목록·플레이어·컨트롤·리모컨을 확인했다.
- 콘솔 오류는 없고 기존 Tailwind CDN 운영 경고만 남아 있다.
- 실제 Firebase·Drive·YouTube 계정·파일·재생 세션과 청각 전환은 미검증이다.
- 루트 최종 검토에서 Engine 소유권, Controller 호출 경계, Port·Adapter 조립, 공개 호환 계약과 CSS 소유권 문서를 다시 확인했다.
- 로컬 정적 서버에서 `index.html`, 8개 CSS, `bootstrap.js`, `appComposer.js`가 모두 HTTP 200으로 제공됐다.
