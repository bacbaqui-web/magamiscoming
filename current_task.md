# 커스텀 탭 스크롤바 디자인 통일 보고서

## 원래 요청

- 커스텀 탭에 표시되는 투박한 스크롤바 디자인을 마감이즈커밍과 맞춘다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 커스텀 iframe에 다크 컬러 스킴을 지정해 브라우저 기본 스크롤바가 밝게 표시되지 않도록 했다.
- iframe 자체에 얇은 스크롤바와 반투명 회색 막대 선호값을 지정했다.
- 같은 출처의 커스텀 페이지에는 마감이즈커밍 본문과 같은 10px 투명 트랙, 둥근 반투명 막대 스타일을 자동 삽입한다.
- 스크롤바에 마우스를 올리면 막대가 조금 밝아지도록 기존 본문 스타일과 통일했다.
- 외부 사이트처럼 내부 문서에 접근할 수 없는 경우에는 오류 없이 다크 컬러 스킴만 적용하도록 처리했다.
- iframe 로딩 전 흰 배경이 보이지 않도록 기본 배경을 어두운 색으로 변경했다.

## 변경 파일

- `src/features/mainTabs.js`: 같은 출처 iframe의 스크롤바 스타일 자동 적용
- `styles.css`: 커스텀 iframe 다크 컬러 스킴과 얇은 스크롤바 설정
- `current_task.md`: 이번 작업 보고서로 갱신

## 검증 결과

### 완료

- 로컬 커스텀 탭에서 iframe의 `color-scheme`이 `dark`로 적용되는 것을 확인
- iframe 내부에 전용 스크롤바 스타일 요소가 삽입되는 것을 확인
- iframe 내부 `scrollbar-width`가 `thin`으로 적용되는 것을 확인
- iframe 내부 막대 색상이 반투명 흰색, 트랙이 투명으로 적용되는 것을 확인
- 브라우저 화면에서 얇고 둥근 회색 스크롤바를 시각 확인
- 검증용 커스텀 탭을 생성해 확인한 뒤 삭제 완료
- `npm run lint` 통과
- Prettier 포맷 적용 완료
- `git diff --check` 통과

### 미확인 및 제한

- 외부 사이트가 자체적으로 강한 스크롤바 스타일을 지정하면 해당 사이트 스타일이 우선될 수 있다.
- 동일 출처 정책으로 외부 iframe 내부 CSS를 직접 수정할 수 없으므로, 외부 페이지에는 브라우저가 지원하는 다크 컬러 스킴 범위까지만 적용된다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`style: 커스텀 탭 스크롤바 통일`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 브라우저마다 스크롤바 렌더링 방식이 달라 두께와 투명도에 약간의 차이가 있을 수 있다.
- 자주 사용하는 실제 외부 커스텀 URL에서도 표시 상태를 확인하는 것을 권장한다.
