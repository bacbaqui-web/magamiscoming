# 월간·주간 달력 디자인 통일 보고서

## 원래 요청

- 월간 달력 디자인을 주간 달력과 통일한다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 월간 달력의 평평한 단일 표 구조를 6개의 주 카드 구조로 변경했다.
- 각 주 카드에 주간 달력과 동일한 짙은 배경, 둥근 모서리, 입체 그림자를 적용했다.
- 월간 제목에 주간 제목과 같은 얇은 글꼴과 숫자 확대 스타일을 적용했다.
- 요일 행의 글자 크기와 색상을 주간 달력의 요일 스타일에 맞췄다.
- 날짜 셀의 내부 여백, 날짜 위치, 일정 높이와 글자 크기를 주간 달력과 비슷하게 조정했다.
- 일요일·토요일·공휴일·오늘 색상은 기존 주간 달력 규칙을 그대로 유지했다.
- 오늘이 포함된 주 카드에 얇은 테두리를 표시해 현재 주를 구분했다.
- 작은 화면에서도 주 카드 구조가 유지되도록 셀 높이와 일정 글자 크기를 조정했다.

## 변경 파일

- `src/features/calendar.js`: 월간 날짜를 6개 주 카드로 나누고 제목 숫자 강조 적용
- `styles.css`: 월간 달력을 주간 카드 디자인과 동일한 시각 체계로 변경
- `current_task.md`: 이번 작업 보고서로 갱신

## 검증 결과

### 완료

- 월간 달력에 6개 주 카드와 42개 날짜 셀이 생성되는 것을 확인
- 월간 제목의 연도와 월 숫자에 주간 제목용 숫자 강조가 적용되는 것을 확인
- 로컬 브라우저에서 주별 카드, 둥근 모서리, 그림자, 주말·공휴일·오늘 강조를 시각 확인
- `npm run lint` 통과
- `npm run format:check` 통과
- `git diff --check` 통과
- 이번 변경으로 발생한 브라우저 콘솔 오류 없음

### 미확인

- 일정이 매우 많이 들어간 실제 계정의 월간 화면은 확인하지 않았다.
- 기존 Tailwind CDN 운영 환경 경고는 이번 변경과 무관하다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`style: 월간 달력 디자인 통일`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 월간 달력의 카드 간격 때문에 전체 세로 길이는 이전 표 형태보다 길어진다.
- 일정이 많은 날짜는 셀 높이 안에서 일부 일정 제목이 아래로 이어질 수 있으므로 실제 데이터로 추가 확인하는 것을 권장한다.
