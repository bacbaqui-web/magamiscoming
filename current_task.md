# 현재 작업 보고

## 최신: 음파에서 직접 드럼 구간 편집

별도 초록 띠를 음파와 합쳤다. 음파 아래 두 핸들을 움직이면 초록 경계가 즉시 따라간다.
자동 분석 구역과 기존 저장·복원·재생 동작은 유지한다. 메인/Lab 공통 적용이다.
입력 중 갱신과 곡 전환 테스트를 추가했으며 실제 포인터 드래그 QA는 아직 미실행이다.

## 최신: 전체 음파·구간 후보

분석기 0.3.0과 메인/Lab의 전체 음파·색상 구역을 구현했다. 인트로/아웃트로는 드럼 앞뒤 후보,
후렴은 반복 스펙트럼과 상대적 에너지 기반 후보다. 확신할 수 없는 벌스·브릿지 등은 일반 구간이다.
실제 로컬 두 곡에서 음파·드럼 경계를 생성했다. 두 곡의 후렴은 기준을 충족하지 않아 표시하지 않았다.
재생 ID 대체 조회와 주황 위치선을 보완했다. 자동 DJ 실행은 이번 범위에 포함하지 않는다.
프런트 100개·백엔드 73개 테스트 통과. 배포된 공용 분석 화면에서 실제 분석 버튼과 음파·주황선
이동을 확인했다. 사용자 기존 재생 탭은 유지했고 검증 탭은 정지·볼륨 복원 후 닫았다.

사용자 승인으로 앞으로 요청된 수정은 검증 후 별도 질문 없이 관련 파일만 커밋·푸시하고
배포 결과를 확인한다. 이번 그래프 변경부터 적용하며 무관한 `current_task 2.md`는 제외한다.

## 최신: 그래프 확대·재생 위치

분석 그래프를 100px로 키우고 초록 구간 선택 띠를 16px로 줄였다. 손잡이 조작 크기는 유지했다.
박자·추정 마디 범례와 주황 재생 위치선·시간 표시를 추가했다. 메인과 Lab에서 기존 재생 시각을
500ms마다 반영한다. 실제 YouTube 재생·손잡이 드래그의 시각 QA는 남아 있다.

## 최신: 별도 링크 없이 로컬 분석

일반 `https://magamiscom.ing/`에서도 이 Mac의 분석 서버를 기본으로 사용하도록 변경했다.
별도 주소 없이 새로고침 후 분석 버튼을 사용한다. Mac 분석 서버 실행과 브라우저 로컬 네트워크
권한이 필요하다. Oracle는 `?mediaAnalysis=oracle`에서만 명시적으로 사용한다.
서버 연결 실패 안내를 보완했으며 큐·10분 제한·사이트 로그인·재생 계약은 유지한다.

## 최신: 분석 큐·남은 대기 수

분석을 누른 곡은 서버 큐에 들어가고 한 곡씩 처리한다. 곡을 바꿔 다른 곡을 추가해도 앞서
등록한 작업은 계속 진행된다. 화면에는 `서버 전체: 분석 중 1곡 · 대기 3곡`처럼 표시한다.
중복 등록·10분 초과 거부를 유지하고 새로고침 후에는 해당 곡의 진행 중 작업을 다시 조회한다.

프런트엔드 95개·서버 65개 테스트와 정적 검사를 확인했다. 로컬 API의 큐 조회·기존 결과
재사용도 검증했다. 실제 로그인 브라우저 클릭 QA와 Oracle의 YouTube 접근 제한 해결은 남아 있다.
로컬 사용: `https://magamiscom.ing/?mediaAnalysis=local` (Mac 분석 프로세스 실행 필요).

로컬·Oracle 서버에 큐 API를 반영했다. Oracle health 200·비로그인 큐 조회 401을 확인했고
기존 5개 서비스는 정상 실행 중이다. 이번 커밋은 관련 두 저장소의 변경만 포함하며
`current_task 2.md`는 보존한다.

## 이전: 로컬 분석 사용 가능

이 Mac에서 분석 서버를 실행했다. `https://magamiscom.ing/?mediaAnalysis=local`로 접속하면
분석 요청이 Oracle 대신 Mac으로 간다. 기존 운영 기본 설정은 변경하지 않았다.

Oracle에서 실패했던 두 곡은 로컬 HTTP API에서 모두 다운로드·분석에 성공했다(약 13초·8초).
Mac backend 테스트 64개와 Ruff 검사를 통과했고 health·CORS·결과 조회를 확인했다.

재실행: `/Users/bacbaqui/Desktop/code/media-analysis-service/scripts/run-local.sh`를 bash로 실행한다.
Mac과 프로세스가 켜져 있어야 하며, 브라우저 로컬 네트워크 권한 요청이 뜨면 허용해야 한다.
로그인 브라우저 버튼 클릭 검증은 아직 남아 있다. Oracle 공개 사용 안정화는 다음 과제로 분리했다.

## 최신: YouTube 다운로드 장애

Node 활성화·EJS 설치 누락을 보완했다. 그러나 실패한 두 곡은 Oracle에서 여전히 YouTube
로그인/봇 확인 응답으로 차단된다. 다운로드 성공으로 보고하지 않는다.
서버 오류를 `youtube_auth_required`로 구분하여 앱 로그인과 별개인 접근 제한임을 안내한다.
기존 10분 제한·로그인 검증·자원 제한은 유지한다. 상세 근거는 `docs/99_recent_task.md`에 기록했다.

## 후속 요청 반영

- 특정 계정 제한을 제거하고 마감이즈커밍에 로그인한 모든 사용자가 분석할 수 있게 변경했다.
- 10분(600초)까지 허용하고 초과 곡은 거부한다. 서버는 다운로드 전 길이를 확인하고
  변환 후 실제 음원 길이도 다시 검사한다. 길이 확인이 불가능한 경우도 거부한다.
- 이전 이메일 허용 대기는 더 이상 필요 없다. 사용자가 두 저장소 커밋·푸시를 승인했다.
  로그인 브라우저 최종 검증은 남아 있다.
- Oracle에 반영했다. 프런트 테스트 89개, 서버 테스트 61개와 lint·format·diff 검사가 통과했다.

아래는 이전 구현 당시 기록이다. 최신 정책은 이 후속 요청과 `docs/99_recent_task.md`가 우선한다.

## 2026-09-04 최신 결과

Oracle E2에 분석 API를 설치했고 실제 213초 곡의 다운로드·분석·결과 조회·cache 재사용에
성공했다. 분석 작업은 78초가 걸렸다. 운영 API health는 200, 익명 요청은 401이다.

프런트엔드 분석 버튼은 Oracle 주소와 기존 Firebase 로그인 토큰을 사용하도록 구현했다.
CPU 25%·메모리 256MB 제한, 저메모리 WAV 분석과 YouTube 다운로드 오류 안내를 추가했다.
일부 곡은 YouTube 접근 제한으로 실패할 수 있다. 기존 5개 서버 서비스는 active를 유지했다.

아직 사용 완료 상태는 아니다. 사용자 로그인 이메일 허용과 프런트엔드 커밋·푸시 승인이
필요하다. 계정 허용 목록은 비어 있으므로 현재는 아무 계정도 원격 작업을 만들 수 없다.
로그인 브라우저에서 분석 버튼을 누르는 최종 검증도 남아 있다.

검증: 프런트 테스트 88개·ESLint·Prettier·diff 검사, Oracle backend pytest 53개·Ruff lint/format 통과.
상세 변경·보호 경계와 배포 상태는 `docs/99_recent_task.md`를 따른다.

## 이전 보고: 로컬 Lab 구현

## 요청

메인 앱 통합 전에 노동요 분석·스마트 이어듣기를 독립적으로 검증할 수 있는 localStorage 기반
Lab 페이지를 만든다.

## 완료 결과

- 분석 서비스는 설정된 정확한 Origin만 CORS로 허용하며 wildcard를 거부한다.
- AppComposer가 Media Analysis Adapter와 Port를 만들고 노동요 AnalysisController에 주입한다.
- 현재 곡의 작업 생성, poll, 결과 조회와 곡 전환 stale 응답 차단을 구현했다.
- 재생바 아래에 분석 상태, BPM·신뢰도, beat/bar marker, 드럼 구간 handle과 저장 UI를 추가했다.
- 자동 분석 결과는 runtime cache에만 두고 수동 구간만 기존 곡 데이터와 Cloud 저장 경로에
  기록한다.
- 현재 곡과 대기 곡의 저장된 수동 구간이 모두 유효하면 계산된 시각에 다음 곡을 시작한다.
  수동값이 없거나 한쪽뿐이거나 범위를 벗어나면 기존 fixed seamless를 그대로 사용한다.
- detected 결과는 편집 참고값이며 실제 자동 재생에는 사용하지 않는다.
- 로컬 구현은 완료했고 오라클 배포는 A1 인스턴스 가용 용량 확보 전까지 대기한다.
- 빈 곡 목록 렌더가 PlaybackController의 빈 load를 다시 호출하던 재귀를 제거했다. 빈 목록의
  분석 Controller도 첫 선택에서 `empty` 상태를 게시한다.
- 로컬 Origin의 Google OAuth `origin_mismatch`를 피하면서 실제 계정 데이터를 시험할 수 있게
  운영 주소의 `?mediaAnalysis=local` 접속에서만 이 Mac의 로컬 분석 API를 사용한다.
- `workmusic-lab.html`에서 로그인 없이 YouTube 목록 추가·재생 Controller·분석·수동 드럼 구간
  보정을 확인할 수 있다. 목록과 수동값은 `magamiscoming.workmusicLab.v1` localStorage에만
  저장하고 Firebase와 메인 앱 저장 데이터는 건드리지 않는다.
- 단일 영상뿐 아니라 공개 YouTube 재생목록 URL도 전체 페이지를 순회해 한 번에 추가한다.
  기존 로컬 목록을 유지하고 같은 영상은 중복 추가하지 않는다.
- 기능 확인용 Lab의 목록이 비어 있으면 지정된 679곡 테스트 재생목록을 자동으로 가져온다.
- 분석 서버에 최대 100곡 batch·SQLite 상태 복원·현재 분석기 결과 cache를 추가했다. Lab에는
  5곡 시험·전곡 분석, 진행률, 중단·재개 UI를 연결했다.
- Lab의 별도 대시보드 스타일을 제거하고 메인 노동요 탭의 800px 패널, 점선 링크 입력, 5단
  커버 흐름, 원형 재생 컨트롤과 회색 분석·목록 패널 디자인에 맞췄다.

## 검증 경계

- backend pytest 42개와 ruff check·format이 통과했다.
- frontend test 87개와 ESLint·Prettier·`git diff --check`가 통과했다.
- Lab에서 목록의 localStorage 복원, 실제 1곡 분석 완료, BPM·confidence·드럼 구간 표시와 수동
  시작 5초 저장·새로고침 복원을 확인했다. 콘솔 오류는 없었다.
- 인앱 브라우저에서 테스트 영상의 YouTube iframe 재생이 거부돼 실제 재생과 두 곡 청각 품질
  QA는 별도 사용자 브라우저 검증으로 남긴다.
- 메인 노동요 빈 상태와 Lab을 같은 1280×720 viewport에서 비교했고 Design QA를 통과했다.
- 공개 재생목록을 실제 입력해 183곡이 한 번에 추가되고 총 184곡으로 표시되는 것을 확인했다.
- 지정 재생목록 첫 5곡을 실제 batch 분석해 4곡 성공·1곡 다운로드 실패를 확인했다. 성공 결과는
  SQLite와 결과 API에서 조회되며 679곡 전체 분석은 아직 시작하지 않았다.

## Git

- 현재 완료 변경은 사용자 요청에 따라 `main` 커밋·푸시 대상으로 검증했다.
