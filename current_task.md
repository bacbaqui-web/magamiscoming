# 커스텀 탭 추가 버튼 위치 변경 보고서

## 원래 요청

- 상단의 `+` 새 탭 버튼을 프로필 화면의 `탭 표시 설정` 영역으로 옮긴다.
- 완료된 커스텀 탭 기능을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 상단 탭 바에서 `+` 버튼을 제거했다.
- 프로필 화면의 `탭 표시 설정` 카드 아래에 `+ 커스텀 탭 추가` 버튼을 배치했다.
- 프로필의 커스텀 탭 관리 카드에 있던 중복 추가 버튼은 제거했다.
- 이름, URL, 아이콘을 입력하는 커스텀 탭 설정 창을 추가했다.
- 링크, 별, 지구본, 폴더, 이미지, 책 아이콘 중 하나를 선택할 수 있게 했다.
- 프로토콜을 생략한 URL에는 자동으로 `https://`를 붙인다.
- `http://`와 `https://` URL만 허용해 위험한 URL 스킴 실행을 차단했다.
- 생성된 커스텀 탭을 누르면 설정한 웹사이트를 새 브라우저 탭으로 열게 했다.
- 프로필 화면에 커스텀 탭 관리 목록을 추가해 수정과 삭제가 가능하게 했다.
- 커스텀 탭 목록을 기존 앱 상태에 포함해 Firebase 또는 Google Drive 저장 흐름으로 동기화되게 했다.
- 기존 일정, 메모, 북마크 등 기능 데이터와 커스텀 탭 설정을 분리해 삭제 시 다른 데이터에 영향을 주지 않게 했다.
- 기존 커스텀 탭 생성·수정·삭제·저장 기능은 그대로 유지했다.

## 변경 파일

- `index.html`: 상단 추가 버튼 제거 및 탭 표시 설정 카드로 버튼 이동
- `styles.css`: 제거된 상단 추가 버튼 스타일 정리 및 이동된 버튼 여백 추가
- `src/features/mainTabs.js`: 프로필의 추가 버튼을 기준으로 설정 창을 열도록 수정
- `src/services/appDataTransforms.js`: 커스텀 탭 상태의 수집·적용·분리·병합 로직 추가
- `src/services/cloudStateHandlers.js`: 커스텀 탭 설정 저장 예약 처리 추가
- `src/services/cloudSyncBackend.js`: 로그인·지연 로딩·로그아웃 시 커스텀 탭 상태 동기화 추가
- `current_task.md`: 이번 작업 보고서로 갱신

## 검증 결과

### 완료

- 로컬 브라우저에서 상단 탭 바의 `+` 버튼이 제거된 것을 확인
- 프로필의 `탭 표시 설정` 카드 안에 `+ 커스텀 탭 추가` 버튼이 표시되는 것을 확인
- 이동된 버튼을 누르면 기존 커스텀 탭 추가 창이 정상적으로 열리는 것을 확인
- `npm run lint` 통과
- `npm run format:check` 통과
- `git diff --check` 통과
- 이번 변경으로 발생한 브라우저 콘솔 오류 없음

### 미확인

- 이번 변경은 버튼 위치만 다루므로 실제 로그인 동기화는 다시 검사하지 않았다.
- 기존 Tailwind CDN 운영 환경 경고는 이번 변경과 무관하다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`feat: 커스텀 링크 탭 추가`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 기능 로직과 저장 구조는 변경하지 않아 기존 커스텀 탭 데이터에는 영향이 없다.
- 프로필 탭은 항상 표시되므로 커스텀 탭 추가 기능에 계속 접근할 수 있다.
