# 현재 작업 보고서

## 원래 요청

파일명이 뒤섞인 CLIP 프로젝트도 정확한 작품 순서로 표시할 수 있도록, CMC 프로젝트를 여는 방식으로 클립 뷰어를 변경한다.

## 원인

- 기존 클립 뷰어는 선택한 폴더의 `.clip` 파일을 파일명 자연 정렬로 표시했다.
- 실제 `바퀴멘터리` 프로젝트에는 `page0001` 다음에 `page0011`이 오거나, 번호가 작은 파일이 작품 뒤쪽에 배치되는 사례가 있다.
- 따라서 파일명만으로는 Clip Studio의 실제 페이지 순서를 확정할 수 없다.
- 정적 웹앱은 사용자가 `.cmc` 파일 하나만 선택했을 때 주변 `.clip` 파일 권한을 자동으로 얻을 수 없다. 이에 버튼은 CMC 프로젝트 열기로 표현하되 CMC와 CLIP가 함께 있는 관리 폴더를 선택하도록 유지했다.

## 완료한 변경

- 클립 뷰어 안내를 `CMC 프로젝트 열기`로 변경했다.
- 관리 폴더에서 `.cmc` 파일을 자동으로 찾도록 했다.
- CMC SQLite의 `Project.ProjectRootCanvasNode`에서 루트 노드를 찾고, `CanvasNode.FirstChildIndex`부터 각 노드의 `NextIndex`를 순회해 실제 페이지 순서를 복원하도록 했다.
- `CanvasNode.LinkPath`와 선택된 `.clip` 파일의 상대 경로를 비교해 연결된 파일만 CMC 순서대로 표시하도록 했다.
- Unicode 파일명을 정규화하고 대소문자 및 경로 구분자를 통일해 macOS 경로 매칭 안정성을 높였다.
- CMC가 여러 개면 연결된 CLIP 파일이 가장 많이 일치하는 CMC를 선택하도록 했다.
- CMC에 등록됐지만 실제 파일이 누락된 페이지 수를 상태에 표시하도록 했다.
- CMC가 없거나 분석할 수 없으면 기존 파일명 자연 정렬을 사용하는 폴백을 유지했다.
- 폴더 드래그앤드롭에서도 CMC 파일을 버리지 않고 함께 분석하도록 수정했다.
- CMC가 SQLite 데이터 앞에 별도 데이터 영역을 포함하는 경우도 처리할 수 있도록 SQLite 시그니처 위치를 탐색한다.

## 변경 파일

- `src/features/clipviewer.js` — CMC 파싱, 페이지 순서 복원, CLIP 경로 매칭 및 폴백 구현
- `index.html` — CMC 프로젝트 열기 안내로 UI 문구 변경
- `current_task.md` — 이번 구현 및 검증 결과 기록

## 검증 결과

### 완료

- `바퀴멘터리/1652화/코믹/1652화.cmc`에서 `FirstChildIndex`/`NextIndex` 순서 복원 확인
- 추가 8개 CMC에서 파일명 정렬과 다른 실제 페이지 순서가 복원되는 것을 확인
- 전체 CMC 607개 및 CLIP 6,706개가 있는 실제 자료 구조 확인
- `npm run lint` 통과
- `npm run format:check` 통과
- `git diff --check` 통과
- 로컬 웹 서버에서 클립뷰어 탭과 새 `CMC 프로젝트 열기` 안내가 정상 렌더링되는 것을 확인
- 페이지 초기화 시 브라우저 콘솔 오류 없음
- 수정 파일 검토 완료

### 미확인

- 자동화 브라우저에서 운영체제 폴더 선택창 제어가 제한되어, 실제 폴더 선택 후 이미지가 렌더링되는 전체 흐름은 아직 수동 확인하지 않았다.
- 이번에 확인한 실제 CMC 표본은 `ProjectInternalVersion 1.1.0`이었다.

## Git 상태

- 브랜치: `main`
- 커밋 완료: 예 (`feat: CMC 순서로 클립 미리보기 표시`)
- 푸시 완료: 예 (`origin/main`)
- 기존 미추적 파일 `current_task 2.md`는 변경하지 않았다.

## 위험 및 남은 사항

- 다른 CMC 내부 버전에서 테이블이나 열 이름이 달라지면 파일명 정렬 폴백이 사용된다.
- 선택한 상위 폴더에 여러 프로젝트가 있으면 현재는 연결 파일이 가장 많이 일치하는 CMC 하나만 표시한다.
- 브라우저 보안상 `.cmc` 파일 하나만 클릭해서 같은 폴더 전체를 자동 승인하는 것은 불가능하므로 관리 폴더를 선택해야 한다.

## 추천 다음 단계

- 브라우저에서 `1652화/코믹`과 순서가 크게 섞인 `1750화/코믹` 폴더를 각각 선택해 화면 표시 순서를 수동 확인한다.
