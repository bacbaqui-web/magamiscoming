# 상단 탭 고정 및 커스텀 탭 스크롤 정리 보고서

## 원래 요청

- 탭 내용을 스크롤해도 상단 탭은 항상 화면 맨 위에 보이게 한다.
- 커스텀 탭 페이지의 옆 스크롤바가 기존 디자인과 다른 원인을 확인하고 정리한다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 상단 탭 바에 `position: sticky`를 적용해 일반 탭 내용을 스크롤해도 화면 위에 고정되도록 했다.
- 고정된 탭 바의 빈 영역이 투명하게 보이지 않도록 검은 배경과 약한 그림자를 적용했다.
- 커스텀 탭이 활성화되면 전용 상태 클래스를 `body`에 추가하고, 일반 탭으로 돌아오면 제거하도록 했다.
- 커스텀 탭에서는 바깥 문서의 세로 스크롤과 하단 푸터를 숨겨 이중 스크롤을 제거했다.
- 커스텀 탭 영역을 상단 탭 바로 아래부터 화면 하단까지 고정했다.
- iframe이 커스텀 탭 영역의 남은 높이를 모두 사용하도록 고정 높이를 제거하고 flex 크기로 변경했다.

## 원인 확인

- 기존 커스텀 탭 iframe은 최소 640px 높이를 사용해 바깥 문서 스크롤과 iframe 내부 스크롤이 함께 생길 수 있었다.
- 외부 URL의 iframe 내부 스크롤바 모양은 동일 출처 정책 때문에 마감이즈커밍 CSS로 직접 변경할 수 없다.
- 대신 마감이즈커밍 바깥 스크롤을 제거해 실제 웹페이지의 스크롤바 하나만 보이도록 정리했다.

## 변경 파일

- `index.html`: 일반 탭 전환 시 커스텀 탭 전용 화면 상태 해제
- `src/features/mainTabs.js`: 커스텀 탭 활성화 시 전용 화면 상태 적용
- `styles.css`: 상단 탭 고정, 커스텀 탭 전체 높이 및 스크롤 구조 정리
- `current_task.md`: 이번 작업 보고서로 갱신

## 검증 결과

### 완료

- 긴 달력 화면을 391px 스크롤한 뒤 탭 바 상단 위치가 `0px`로 유지되는 것을 확인
- 커스텀 탭에서 바깥 문서 높이와 화면 높이가 모두 `720px`인 것을 확인
- 커스텀 탭에서 바깥 세로 스크롤이 숨겨지고 푸터가 표시되지 않는 것을 확인
- 커스텀 탭 영역이 탭 하단 `55px`부터 화면 하단 여백 전 `704px`까지 채우는 것을 확인
- iframe이 커스텀 탭 헤더 아래부터 영역 하단까지 채우는 것을 확인
- 검증용 커스텀 탭을 생성해 실제 화면을 확인한 뒤 삭제 완료
- `npm run lint` 통과
- Prettier 포맷 적용 완료
- `git diff --check` 통과

### 미확인 및 제한

- 외부 사이트가 자체적으로 지정한 iframe 내부 스크롤바 디자인은 변경할 수 없다.
- iframe 표시를 차단하는 사이트는 기존과 마찬가지로 커스텀 탭 안에서 열리지 않을 수 있다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`fix: 상단 탭과 커스텀 화면 고정`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 모바일 브라우저의 주소창 높이 변화는 `100dvh` 기준으로 대응하지만 실제 기기에서 한 번 더 확인하는 것을 권장한다.
- 실제 모바일 기기에서 주소창이 접히고 펼쳐질 때의 높이를 추가 확인하는 것을 권장한다.
