# 메인 탭 표시 설정 추가 및 배포 보고서

## 원래 요청

- 프로필 화면에서 상단 기능 탭을 켜고 끌 수 있게 한다.
- 탭을 끄더라도 탭 안에 저장된 내용은 삭제되지 않게 한다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 프로필 화면에 `탭 표시 설정` 카드를 추가했다.
- 달력, 뽀모도로, 메모, 북마크, 노동요, 클립뷰어를 각각 스위치로 표시하거나 숨길 수 있게 했다.
- 설정에 다시 접근할 수 있도록 프로필 탭은 항상 표시되게 유지했다.
- 숨김 처리는 상단 탭 버튼에만 적용하고 각 기능의 데이터와 화면 요소는 삭제하지 않는다.
- 숨긴 탭 목록을 `hiddenMainTabs` 상태로 기존 앱 데이터에 포함했다.
- Firebase 사용 시 달력 문서에, Google Drive 방식에서는 `calendar.json`에 숨김 설정이 함께 저장되게 했다.
- 로그인, 지연 데이터 로딩, 로그아웃 과정에서 탭 표시 상태가 올바르게 적용 또는 초기화되게 했다.
- 기존 저장 데이터에 숨김 목록이 없으면 모든 탭을 표시하도록 호환성을 유지했다.

## 변경 파일

- `index.html`: 프로필 탭 표시 설정 UI와 탭 표시 제어 로직 추가
- `styles.css`: 설정 카드, 반응형 스위치 UI, 숨김 탭 스타일 추가
- `src/services/appDataTransforms.js`: 숨김 탭 상태의 기본값, 수집, 적용, 분리, 병합 로직 추가
- `src/services/cloudStateHandlers.js`: 탭 표시 설정 저장 예약 처리 추가
- `src/services/cloudSyncBackend.js`: 로그인·지연 로딩·로그아웃 시 숨김 설정 동기화 추가
- `current_task.md`: 이번 작업과 배포 결과 기록

## 검증 결과

### 완료

- 로컬 브라우저에서 프로필 화면에 6개 탭 스위치가 표시되는 것을 확인
- 메모 스위치를 끄면 상단 메모 탭 버튼만 숨겨지는 것을 확인
- 프로필 탭과 프로필 화면이 계속 유지되는 것을 확인
- 일정, 메모, 노동요 샘플 데이터와 숨김 설정을 분리·병합한 뒤 데이터가 보존되는 자동 검사 통과
- `npm run lint` 통과
- `npm run format:check` 통과
- `git diff --check` 통과
- 브라우저 콘솔에서 이번 변경으로 발생한 오류 없음

### 미확인

- 실제 Google 계정에서 저장한 뒤 다른 기기에서 설정을 다시 불러오는 과정은 수행하지 않았다.
- 기존 Tailwind CDN 운영 환경 경고는 이번 변경과 무관하다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`feat: 프로필 탭 표시 설정 추가`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 숨김 설정은 기존 데이터 필드와 분리되어 있으므로 탭을 꺼도 내부 데이터가 삭제될 위험은 낮다.
- 배포 후 실제 로그인 상태에서 탭을 숨기고 새로고침했을 때 설정이 복원되는지 확인하는 것을 권장한다.
- Google Drive를 Firebase Storage로 전환하는 작업은 이번 커밋에 포함하지 않았으며 추후 별도 작업으로 진행한다.
