# 다음 곡 수동 이동 이어듣기 적용 보고서

## 원래 요청

- 노동요에서 다음 곡 버튼을 누르면 즉시 곡을 교체하지 않고 이어듣기를 적용해 전환한다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 원인

- 자동 곡 종료는 준비된 보조 플레이어를 사용하는 이어듣기 전환 경로를 사용했다.
- 본 화면과 하단 리모컨의 다음 곡 버튼은 `playWorkMusicAt()`을 직접 호출해 플레이어를 즉시 다시 만들었다.
- 이 때문에 이어듣기가 켜져 있어도 수동 다음 곡 이동에는 크로스페이드가 적용되지 않았다.

## 완료한 작업

- 수동 다음 곡 이동을 담당하는 `playNextWorkMusicWithSeamless()` 함수를 추가했다.
- 이어듣기가 켜져 있고 다음 곡 보조 플레이어가 준비된 경우 기존 크로스페이드 전환 함수를 호출하도록 했다.
- 메인 노동요 화면과 하단 리모컨의 다음 곡 버튼을 모두 새 전환 함수에 연결했다.
- 크로스페이드 진행 중 다음 곡 버튼을 연속으로 눌러도 전환이 중복 실행되지 않도록 했다.
- 이어듣기가 꺼져 있거나 다음 곡 준비가 완료되지 않은 경우에는 기존 즉시 이동을 유지했다.
- 이전 곡 버튼의 기존 동작은 변경하지 않았다.

## 변경 파일

- `src/features/workmusic.js`: 다음 곡 버튼에 이어듣기 크로스페이드 경로 적용
- `current_task.md`: 이번 작업 보고서로 갱신

## 검증 결과

### 완료

- 메인 다음 곡 버튼이 새 이어듣기 전환 함수에 연결된 것을 확인
- 하단 리모컨 다음 곡 버튼도 같은 전환 함수에 연결된 것을 확인
- 이어듣기 활성화, 겹침 시간, 보조 플레이어와 다음 곡 인덱스를 모두 확인한 뒤 전환하도록 조건을 검토
- 전환 시작 또는 진행 중에는 재실행하지 않는 방어 조건을 확인
- `npm run lint` 통과
- Prettier 포맷 적용 완료
- `git diff --check` 통과

### 미확인 및 제한

- 로컬 테스트 환경에 실제 노동요 목록과 YouTube 재생 세션이 없어 두 곡의 음량이 겹치는 청각 검증은 하지 못했다.
- 이어듣기 설정이 0초이거나 꺼져 있으면 기존처럼 즉시 다음 곡으로 이동한다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`fix: 다음 곡 버튼에 이어듣기 적용`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 실제 계정에서 이어듣기 시간을 3~10초로 설정하고 다음 곡 버튼을 눌러 크로스페이드 음량을 확인하는 것을 권장한다.
- YouTube 플레이어가 다음 곡을 아직 준비하지 못한 아주 짧은 순간에는 안전하게 기존 즉시 이동으로 대체된다.
