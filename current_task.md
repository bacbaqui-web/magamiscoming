# 탭별 설정 행과 월간 달력 추가 배포 보고서

## 원래 요청

- 프로필의 탭 표시 설정을 탭당 한 줄 구조로 바꾼다.
- 각 행에 설정 버튼과 ON/OFF 스위치를 둔다.
- 달력 설정에서 월간 보기와 주간 보기를 선택할 수 있게 한다.
- 완료된 변경을 커밋하고 `main` 브랜치에 푸시한다.

## 완료한 작업

- 프로필의 6개 기본 탭을 각각 한 줄씩 표시하도록 변경했다.
- 각 행을 `탭 이름 | 설정 | ON/OFF` 구조로 구성했다.
- 기존 탭 표시 스위치와 숨김 데이터 보존 기능을 유지했다.
- 달력 행의 설정 버튼을 기존 달력 설정 창과 연결했다.
- 다른 탭의 설정 버튼은 향후 확장을 위해 배치하고 현재는 별도 설정 항목이 없다는 안내를 표시한다.
- 달력 설정 창에 `주간 보기`와 `월간 보기` 선택 항목을 추가했다.
- 월간 달력에 월 제목, 이전·다음 달 이동, 요일 행, 42개 날짜 셀, 공휴일과 일정 표시를 구현했다.
- 월간 보기에서도 날짜 클릭으로 일정 추가, 일정 클릭으로 완료 처리와 편집이 가능하도록 기존 동작을 재사용했다.
- 마우스 휠과 위아래 방향키가 주간 보기에서는 주 단위, 월간 보기에서는 월 단위로 이동하게 했다.
- 달력 보기 모드를 기존 앱 상태와 Firebase/Google Drive 저장 흐름에 포함했다.
- 이전 저장 데이터에 보기 모드가 없으면 기존 주간 보기를 기본값으로 사용한다.

## 변경 파일

- `index.html`: 탭별 한 줄 설정 UI와 달력 보기 방식 선택 항목 추가
- `styles.css`: 탭 설정 행과 월간 달력의 데스크톱·모바일 스타일 추가
- `src/features/calendar.js`: 월간 달력 렌더링, 월 이동, 보기 전환 및 설정 연결 추가
- `src/services/appDataTransforms.js`: 달력 보기 모드의 기본값, 수집, 적용, 분리, 병합 로직 추가
- `src/services/cloudStateHandlers.js`: 달력 설정 저장 예약 처리 추가
- `src/services/cloudSyncBackend.js`: 로그인·지연 로딩·로그아웃 시 달력 보기 모드 동기화 추가
- `current_task.md`: 이번 작업과 배포 결과 기록

## 검증 결과

### 완료

- 프로필에서 6개 탭이 각각 `이름 | 설정 | ON/OFF` 한 줄로 표시되는 것을 확인
- 프로필의 달력 설정 버튼으로 달력 설정 창이 열리는 것을 확인
- 주간 보기에서 월간 보기로 전환되는 것을 확인
- 월간 달력에 42개 날짜 셀이 생성되는 것을 확인
- `2026년 8월`에서 다음 달 버튼으로 `2026년 9월` 이동 확인
- 월간 보기에서 다시 주간 보기로 전환하고 5개 주간 행이 복원되는 것을 확인
- 달력 보기 모드와 기존 일정 데이터의 저장·복원 테스트 통과
- 보기 모드가 없는 기존 데이터가 주간 보기로 열리는 호환성 테스트 통과
- `npm run lint` 통과
- `npm run format:check` 통과
- `git diff --check` 통과
- 이번 변경으로 발생한 브라우저 콘솔 오류 없음

### 미확인

- 실제 로그인 계정에서 다른 기기로 달력 보기 설정이 동기화되는 과정은 수행하지 않았다.
- 기존 Tailwind CDN 운영 환경 경고는 이번 변경과 무관하다.

## Git 상태

- 브랜치: `main`
- 커밋: 완료 (`feat: 월간 달력과 탭별 설정 추가`)
- 푸시: 완료 (`origin/main`)

## 위험 및 권장 다음 단계

- 월간 달력에 일정이 매우 많으면 작은 화면에서 일정 제목이 축약될 수 있다.
- 다른 탭의 설정 버튼은 현재 안내만 표시하며, 이후 기능별 설정을 같은 구조로 확장할 수 있다.
- 배포 후 로그인 상태에서 월간 보기를 선택하고 새로고침했을 때 설정이 복원되는지 확인하는 것을 권장한다.
